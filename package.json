{
    "name": "void-monorepo",
    "version": "0.0.0",
    "author": "marigold",
    "devDependencies": {
        "@biomejs/biome": "^2.4.6",
        "@rollup/plugin-typescript": "^12.3.0",
        "@types/bun": "^1.3.11",
        "rollup": "^4.57.1",
        "rollup-plugin-dts": "^6.3.0",
        "tslib": "^2.8.1"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "description": "",
    "private": true,
    "scripts": {
        "lint": "biome check --max-diagnostics=159",
        "build": "rollup -c",
        "test": "bun test --concurrent --randomize --rerun-each 1",
        "testCompiler": "bun run test ./packages/compiler",
        "testCov": "bun run test --coverage --coverage-reporter=lcov --coverage-dir=coverage"
    },
    "workspaces": [
        "./packages/runtime",
        "./packages/compiler"
    ]
}
