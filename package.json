{
    "name": "void-monorepo",
    "workspaces": [
        "packages/void"
    ],
    "scripts": {
        "lint": "biome check",
        "build": "bun run --filter \"*\" --parallel build",
        "test": "bun test --concurrent --randomize"
    }
}
